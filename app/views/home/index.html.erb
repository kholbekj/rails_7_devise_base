<%= button_to(
  "Log Out",
  destroy_user_session_path,
  method: :delete
) if current_user %>
<div>
  <h1 class="text-xl font-semibold text-slate-900">Hehe, hi, let's pick some stuff :3</h1>
</div>
